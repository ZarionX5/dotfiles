export FONT_FAMILY=${FONT_FAMILY:-CommitMono}

export GIT_USER_NAME=$(git config --global user.name)
export GIT_USER_EMAIL=$(git config --global user.email)
export GITHUB_USER="${GITHUB_USER:-ZarionX5}"
export GITLAB_USER="${GITLAB_USER:-loek.n.v}"
